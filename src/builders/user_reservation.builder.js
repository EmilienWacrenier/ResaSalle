const db = require('../config/db.config');
const Op = require('Sequelize').Op;

module.exports.addUserToReservation = function (req){
    return new Promise(async(resolve, reject) => {
        try{

        } catch(err) {
            console.log(err);
        }
    });
}